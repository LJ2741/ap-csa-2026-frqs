
# Question 2 

### My Approach
When I first read the prompt for this frq I knew that at least 2 double variables would be needed to hold the values of the current capacity and the max capacity as I would need to know the max capacity to create the updateAmount method. 

### Course Connections
In class the constructor of a class had always been used to set initial values so I knew that was how I needed to set them in this class.

### What Gave Me Trouble
When creating the updateAmount method I wasn’t sure if I needed to have my check for the minimum value before or after I subtracted the current value by the parameter. After I ran some test cases on paper I created the method correctly.

### What I Would Do Differently
In my solution I used 3 double variables. I think the third variable, minAmount, is not necessary as it is only used one time in the class and the line it is used in could be replaced with a multiplication of 0.25.
